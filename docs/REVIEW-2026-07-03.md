# Scribe2 全面评审报告(2026-07-03)

> 方法:8 维度(store/memory/pipeline/llm/prompts/server-web/cli-config/tests)独立评审 agent 并行精读,每条发现由 1-2 名独立“怀疑者”对抗复核(拿不准判驳回);共 62 个 agent、约 390 万 token。
> 基线:unit 138 绿 / typecheck 0 错 / live 5 绿(三个 Gate 剧本)/ web 构建通过。
> 结果:**36 条确认,5 条指控被驳回**。严重度为验证者复核后的最终定级。

统计:critical 2 / major 17 / minor 16


## CRITICAL

### 1. [pipeline] 落盘阶段非原子:extractor 产出非法角色名时,章节已写、记忆半写、无 commit,脏文件会被下一章 commitAll -A 收编

- **位置**:`src/engine/write.ts` 262-284(配 src/store/book.ts:257-272、src/store/git.ts:45)
- **证据**:write.ts 注释宣称『任何一步失败都不落盘不 commit』,但落盘段本身没有原子性:先 `store.writeChapter(chapterNo, text)`(262)、`writeSummary`(263),然后才逐个 `store.upsertCharacter({ name: stripNew(name), ... })`(268-270)。`stripNew` 只去掉『(新)』后缀,不清洗文件名非法字符;book.ts:257 直接 `this.p("角色", `${input.name}.md`)`,Windows 上名字含 `:` `*` `?` `"` 等时 `fs.writeFileSync` 抛 EINVAL;含 `/` 时 writeFile 的 `mkdirSync(dirname, {recursive:true})`(book.ts:162)会静默建子目录写进 `角色/A/B.md`(listCharacters 非递归 readdir 看不到,`../` 甚至可写出书目录)。抛错点在章节与摘要已落盘之后、`commitAll` 之前。
- **影响**:写坏书:异常后工作区留下 已写正文+摘要+部分角色文件,伏笔/时间线/索引全没更新,且未 commit。`listChapters()` 按 `章节/NNN.md` 判定该章『已完成』,用户从下一章续写时,writeChapter 成功后的 `commitAll(add -A)` 把这份半套记忆连同下一章一起提交,记忆链从此带着一个只有正文没有记忆的章,后续检索/摘要全断。
- **建议修法**:落盘前统一清洗实体名(拒绝或转义 `/\:*?"<>|` 与 `..`,MemoryDelta 解析后即校验);把整批落盘包在 try 里,失败时对工作区做 `git checkout -- . && git clean -fd`(或先写临时区再原子搬运),保证『不落盘不 commit』真正成立。
- **验证摘录**:逐行核对后确认发现属实,未能驳倒。

证据链全部成立:
1. src/engine/write.ts:115 注释宣称「任何一步失败都不落盘不 commit」,但落盘段(262-284)本身无原子保护:262 `store.writeChapter`、263 `store.writeSummary` 先写,268-270 才逐个 `store.upsertC…

### 2. [pipeline] fixLatest reset 后无失败回滚,且 writeMany 对 guardAudit 未做 try——修复途中任何一步失败即崩溃并把书留在『少一章+脏工作区』

- **位置**:`src/engine/many.ts` 45-48、74、123-125
- **证据**:fixLatest:`resetToBefore(store.dir, ...)`(45,git.ts:64 是 `reset --hard`)→ `store.addIssues(keep)`(46,写问题.md 但不 commit)→ `writeChapter(...)`(48)。writeChapter 若在规划/写作/lint 双败/抽取任一步抛错(lint 双败必抛,见 write.ts:240),没有任何 catch 恢复原 commit。同时 writeMany 里 `await guardAudit(store, deps, no)`(124)在 try/catch(111-120)之外,guardAudit 内 74 行调用 fixLatest 的异常直接穿出 writeMany。
- **影响**:连写到第 5/10 章护栏触发自动修复时一旦修复失败:最新章正文+记忆+该 commit 已被 hard reset 掉(只剩 reflog 可救,用户无从知晓),问题.md 处于未提交脏态,writeMany 不返回 ManyResult 而是整个抛异常——既崩溃又丢章;下一次任意 commitAll -A 会把这个中间态永久提交。
- **建议修法**:fixLatest 在 reset 前记录 `rev-parse HEAD`,writeChapter 失败时 `reset --hard` 回原 HEAD 并 rebuildIndex 后再把错误往上抛;writeMany 把 guardAudit 也包进 try,失败转成 `{completed, stoppedAt, reason}` 返回。
- **验证摘录**:逐行核实全部属实。(1) many.ts:45-48 fixLatest 先 resetToBefore（git.ts:64 为 `git reset --hard` 到目标 commit 的父提交，最新章正文+记忆+该 commit 即刻消失，仅 reflog 可救），再 addIssues 写问题.md（不 commit），随后裸调 writeChapt…


## MAJOR

### 3. [cli-config] checkBudget 章均成本把 onboard/chat/audit/revise 成本混进分子,章数做分母,估算系统性高估导致误拒

- **位置**:`src/cli/index.ts` 57-67
- **证据**:`const summary = summarizeUsage(store.dir); const written = store.listChapters().length; const estimate = (summary.totalCostUsd / written) * chapterCount;` 而 deps.ts:46-53 的 onUsage 把**所有角色**(含 chatter,即 chat/onboard 对话)都 recordUsage 进同一账本;status 命令(cli/index.ts:352-354)也按 byRole 打印证实 chat/audit 同账。分母却只有已写章数。
- **影响**:用户建书阶段长聊(onboard/chat 便宜但轮次多)后,totalCostUsd 里非写作成本被摊到极少的已写章数上,章均成本虚高数倍;`write 5..30` 时 estimate 超 singleBudgetUsd 被直接拒绝(budget_exceeded),实际写作成本远低于估算。写得越少、聊得越多,误差越大。(首书 written===0 的除零已被 60 行提前 return 挡掉,该点无问题。)
- **建议修法**:估算分子只取写作管线角色(writer/planner/extractor)的成本,或 summarizeUsage 按 role 过滤后再除以章数;至少排除 chatter。
- **验证摘录**:指控核实成立。cli/index.ts:57-61 的 checkBudget 用 usage.jsonl 全量 totalCostUsd 除以已写章数;而 deps.ts:46-53 的 onUsage 把 chat/onboard/audit/revise 的调用全部记入同一账本(chat.ts:69、onboard.ts:125/140、audit.t…

### 4. [cli-config] costOf 对所有 provider/model 一律按 DeepSeek 价目记账,非 DeepSeek 模型成本可差两个数量级,预算护栏整体失真

- **位置**:`src/deps.ts` 13-23
- **证据**:`const PRICE = { promptMiss: 0.28, promptCacheHit: 0.028, completion: 0.42 };` costOf 无视 usage 对应的 model/provider;而 config.ts 的 providers 是任意 `z.record(...)`,roles 可指向任何 provider/modelId。buildDeps:50-52 把这个 costUsd 写进账本。
- **影响**:用户按 SPEC 配 GPT/Claude 级模型(输入输出单价约为 DeepSeek 的 10~100 倍)时,账本记的钱少记两个数量级;checkBudget(cli/index.ts:57)和 writeMany 的 runBudgetUsd/costProbe(cli/index.ts:254-257)全部基于这本假账,singleBudgetUsd 形同虚设——连写几十章可能实际烧掉预算的百倍真金白银才停。注释自称『只求量级正确』,但对非 DeepSeek 恰恰量级不对。
- **建议修法**:价目随 modelId/provider 走:config.json 里给 provider 或 role 配单价字段,未配价目的模型在 write/writeMany 前警告『成本估算不可信』而不是静默按 DeepSeek 记。
- **验证摘录**:逐行核实,指控属实,复现路径完整可达:

1. 价目硬编码且无模型维度:src/deps.ts:14-23,`const PRICE = { promptMiss: 0.28, promptCacheHit: 0.028, completion: 0.42 }`,`costOf(usage)` 签名只收 Usage,完全不看 model/provider。g…

### 5. [llm] 重试语义被 ai SDK 内层默认重试架空:未传 maxRetries:0,双层重试叠加且 RetryError 吞掉 statusCode

- **位置**:`src/llm/call.ts` 41-45, 68-73
- **证据**:streamCall/generateCall 调 streamText/generateText 时只传了 model/messages/abortSignal:`streamText({ model: input.model, messages: input.messages, abortSignal: input.abortSignal })`。ai@4 的 generateText/streamText 自带默认 maxRetries=2(内置指数退避,对 429/5xx 重试),耗尽后抛 AI_RetryError(message 形如 "Failed after 3 attempts. Last error: ..."),原始 statusCode 不在顶层。外层 retry.ts 的 classifyLlmError 依赖 `err.statusCode ?? err.status`(retry.ts:16)和 message 子串。
- **影响**:1) 429 场景最坏内层 3 次 × 外层 4 次 = 12 次真实请求,退避节奏也不再是 SPEC L275 约定的策略;2) 5xx 场景更糟:内层耗尽后抛 RetryError,无 statusCode,message 里通常也没有 "timeout" 等关键词(如 "Internal Server Error"),被外层归为 unknown → 外层 5xx 重试策略实际失效。自研的 retry.ts 策略等于被 SDK 内层覆盖/扭曲。
- **建议修法**:给 streamText/generateText 显式传 `maxRetries: 0`,让 retry.ts 成为唯一重试层;或反过来干掉外层、只信 SDK 内层,但那样丢失 SPEC 的按错误类分策略。同时在 classifyLlmError 里解包 AI_RetryError(取 lastError)兜底。
- **验证摘录**:逐行核对确认指控属实:

1. **代码事实**(D:/desktop/scribe2/src/llm/call.ts:41-45, 68-73):streamText/generateText 均只传 `model/messages/abortSignal`,全仓 grep `maxRetries` 只命中 retry.ts 自己的策略表,确无任何地方向 …

### 6. [memory] issue 块在关键词检索模式下永远无法被检索到,"open 全部注入"的配额契约是死代码

- **位置**:`src/memory/chunks.ts` 90(配合 retrieve.ts:5,14,122;audit.ts:18-21)
- **证据**:chunks.ts:90 issue 块的 keys 只有一个元素:`keys: [issue.type]`,而 issue.type 是 audit.ts:18-21 的英文枚举("continuity"/"character"/"foreshadow"…)。中文查询实体(角色名/地名/伏笔标签)与这些英文词双向子串永不命中,keywordScore 恒 0。retrieve.ts:122 `if (relevance < threshold) continue;`——关键词模式下 relevance = 0.6*0 = 0 < 0.15,issue 块全部出局;retrieve.ts:14 给 issue 配 `Number.POSITIVE_INFINITY` 配额、retrieve.ts:5 注释宣称「issue 不限量(open 全部注入)」,与实际行为直接矛盾。SPEC §2.4 配额表也写明 issue≤全部 open。CLAUDE.md 明确 live 环境(DeepSeek 无 embeddings 端点)全程跑关键词模式,即产品默认配置下该类型 100% 不可检索。向量模式下需 cos≥0.33 才能单独救活,不确定实际达标率。写章路径不受影响(open 问题走 §2.5 固定层,write.ts 直接传 store.listOpenIssues()),受害的是 chat 与 audit:audit.ts:126 每次审查后还专门 updateChunks 索引新 issue,但这些块在检索层(audit.ts:65-70、chat.ts:42-47)永远捞不回——「发现→规避→复审」闭环在聊天/复审侧断裂,整套 issue 索引机制是无效功。
- **影响**:聊天问「这本书还有哪些未解决的问题」检索层永远给不出 open issue;护栏/审查的检索资料里也永远缺已知问题,auditor 每轮从零发现、重复报告(仅靠 stableId dedup 兜底不翻倍)。
- **建议修法**:chunks.ts 给 issue 块补充有效 keys:至少加入 note 中出现的实体(可用 entitiesFromText 反查)或中文类型词+章号;或者在 retrieve.ts 里让 issue 类型跳过阈值(与注释和 SPEC 表述对齐,open 恒注入)。二选一并同步修正注释。
- **验证摘录**:逐行核对确认:chunks.ts:90 issue 块 keys 确为单元素英文枚举(audit.ts:18-20 → issueInputOf → addIssues → 问题.md → 读回,全链路英文);retrieve.ts:59-67 双向子串匹配对中文实体×英文枚举永不命中,kw 恒 0;关键词模式 relevance=0.6*0=0<0.15,…

### 7. [pipeline] guardAudit 第二轮审查结果被整体丢弃:修复后的 warning 与历史章 critical 既不入问题.md也不停下

- **位置**:`src/engine/many.ts` 75-83
- **证据**:自动修复后 `const second = await collectAuditIssues(...)`(75),只取 `still = second.issues.filter(i => i.severity === "critical" && i.chapterNo === latest)`(76)。`still` 为空即 `return null`(82)——second.issues 中的 warning 和 `chapterNo < latest` 的 critical 全部丢弃,不 addIssues、不 commit、不停下。第一轮 issues 也只通过 fixLatest 的 addIssues 带回,而第二轮完全没有归档路径。
- **影响**:违背 SPEC §3.3 两条契约:『warning → 全部写入 问题.md(open),后续章组装时自动注入规避』和『critical 指向更早章 → 停下报告人工选择』。修复动作本身可能引入的历史矛盾被静默放行,后续章失去规避信息,问题越滚越大。
- **建议修法**:第二轮结果复用第一轮的三分支逻辑(至少:historical critical → addIssues+commit+停;其余 issues 一律 addIssues+commit),而不是只看最新章 critical。
- **验证摘录**:逐行核对 many.ts:70-83 与 audit.ts、SPEC §3.3(147-154 行)确认指控成立。criticalLatest 分支中,fixLatest 后第二轮 collectAuditIssues 的结果只用于 filter 最新章 critical(76 行);still 为空即 return null(82 行),second.is…

### 8. [pipeline] 弧覆盖范围用 `arcNos.length * 10` 计算,弧压缩吞错留下编号空洞后,对应 10 章的中程摘要永久消失

- **位置**:`src/engine/write.ts` 176、189-195、301-307
- **证据**:`const arcCovered = arcNos.length * 10`(176),midSummaries 只注入 `n > arcCovered`(191)。compressArc 失败仅 `console.warn`(304-305)不重试;第 10 章压缩失败后第 20 章照常写出 弧/002.md,此时 `listArcs()=[2]`,`arcCovered=1*10=10`——第 1-10 章既没有弧纲要(001 缺失),又因 `n > 10` 不成立被排除在中程摘要之外。且 warn 文案称『可稍后用 reindex/重写触发』,但 reindex 路径并不会补跑 compressArc,空洞无自愈机制。
- **影响**:固定层出现 10 章记忆空洞:这些章的信息只剩概率性的检索层(预算紧张时还最先被裁),长跑几百章时正是失忆事故的形状,违背 §2.5『弧未覆盖的章摘要注入』意图。
- **建议修法**:`arcCovered` 改为按实际存在的弧计算连续覆盖(如 `Math.max(0, ...arcNos) * 10` 且校验无空洞,或直接用『章 n 是否被某个存在的弧区间覆盖』判定);每章开头检查 `chapterNo > listArcs().length*10 + 10` 时补跑缺失弧。
- **验证摘录**:逐行核对属实:write.ts:176 用 arcNos.length*10 计算覆盖,弧编号出现空洞(如 001 缺失、002 存在)时 1-10 章既无弧纲要注入又因 n>10 不成立被排除出中程摘要,固定层出现 10 章空洞。compressArc 失败路径可达(301-307 仅 warn,第 20 章 compressArc 对缺失的上一弧有 in…

### 9. [pipeline] lint 双败留下的 NNN.draft.md 无人清理,会被之后任意 commitAll -A 顺手提交进无关 commit

- **位置**:`src/engine/write.ts` 238-243(配 src/store/git.ts:44-49)
- **证据**:双稿 lint 失败时 `fs.writeFileSync(章节/NNN.draft.md)` 后抛错(238-243),此后代码里没有任何路径删除该草稿。`commitAll` 是无差别 `git add -A`(git.ts:45)。用户人工裁决后重写该章成功、或连写继续到下一次 audit commit(many.ts:66/79/87)时,草稿被收进 `chNNN: 写作` / `audit: ...` 等无关 commit,永久入库。另:草稿用裸 `fs.writeFileSync` 而非 store.writeFile,fixLatest 的 hard reset 后 章节/ 目录可能因 git 不跟踪空目录而不存在(book.ts:160 注释自证),此时 ENOENT 会掩盖真正的 lint 失败原因。
- **影响**:违背『一章一个 commit,正文与记忆同 commit』契约,被否决的废稿混进书史;rollback/审阅/导出都可能带上 .draft.md。首章即双败的 fixLatest 场景下还会用 ENOENT 替换掉本应报告的 lint_failed 错误。
- **建议修法**:writeChapter 成功落盘时(及 commitAll 前)删除同章 .draft.md;草稿改用 store 的 writeFile(自动补建父目录);或把草稿写到不入 git 的 .index/ 之类目录。
- **验证摘录**:逐行核对确认:write.ts:238-239 双稿 lint 失败时用裸 fs.writeFileSync 写 章节/NNN.draft.md 后抛错,全仓无任何删除该草稿的代码或测试;git.ts:45 commitAll 是无差别 git add -A,书目录 .gitignore 仅含 .index/(book.ts:139),不忽略 *.draft…

### 10. [prompts] extract-memory 要求伏笔回收"与现有伏笔精确同名",但现有伏笔标签从未注入该提示词,回收契约无法履行

- **位置**:`prompts/extract-memory.md` 15;src/engine/write.ts:98-104, 247-253
- **证据**:prompts/extract-memory.md:15 规定「foreshadowPaid:…的旧伏笔的 label(必须与现有伏笔精确同名)」。但渲染调用点 write.ts:247-253 只传 章号/记录规则/角色名录/本章规划/正文 五个槽,没有任何现有伏笔清单;且 write.ts:98-104 的 planAsText 只拼 goal/scenes/charactersOnStage,把 plan.foreshadowToTouch 丢掉了(对比 context.ts:89 给 writer 的 renderPlanBlock 是包含「要触碰的伏笔」的)。store/book.ts applyForeshadow(363-386)按 normalizeLabel 精确匹配(仅全半角/空白归一),对不上就静默不置 paid。
- **影响**:extractor 只能从正文猜 label,几乎不可能与档案里的规范 label 逐字一致 → 已回收伏笔长期滞留 active,plan-chapter 的 {{伏笔清单}} 持续把已回收伏笔喂给规划器,后文反复"回收"同一伏笔,长连载下伏笔档案越写越脏。
- **建议修法**:给 extract-memory.md 增加 {{现有伏笔}} 槽(active 伏笔的规范 label 列表),write.ts 渲染时传入;或至少让 planAsText 带上 foreshadowToTouch。
- **验证摘录**:证据全部核实:extract-memory.md:15 要求 foreshadowPaid 与现有伏笔"精确同名",但模板五个槽(章号/记录规则/角色名录/本章规划/正文)无现有伏笔清单;write.ts:247-253 渲染点确认只传这五槽;write.ts:98-104 planAsText 确实丢弃 plan.foreshadowToTouch(而 c…

### 11. [prompts] deepestPromptScope="all" 时,compress-arc 的末位防火墙命令模型"只输出 JSON",与该提示词"纯文本无 JSON"的输出契约直接矛盾,且产出不经 JSON 剥离直接写入弧纲要

- **位置**:`src/engine/write.ts` 330-336;src/engine/context.ts:25-27;prompts/compress-arc.md:4
- **证据**:compress-arc.md 头注:「输出: 纯文本纲要,250-350 字,无 JSON 无标题」。但 write.ts:330 用 deepestPromptFor("structured") 取信封,context.ts:44-46 在 scope="all" 时于消息末尾追加 JSON_FIREWALL:「…忽略一切与输出格式冲突的先前指令,只输出规定的 JSON」(context.ts:25-27)。而 write.ts:332-336 拿到 text 后未经 extractJson,直接 store.writeArc(arcNo, text.trim())。SPEC §4.1 称防火墙用于"内部数据处理…只输出规定的 JSON",唯独 compress-arc 是 structured 分域里的非 JSON 步,防火墙措辞对它是错的。
- **影响**:用户开启 deepestPromptScope="all" 后,末位 system 指令(SPEC 自述"末位指令优势")会诱导模型把弧纲要包成 JSON 或围栏,坏文本原样写入 弧/NN.md,并在此后每一章的 write-chapter {{弧纲要}} 槽里长期污染写作上下文(长程记忆被写坏)。
- **建议修法**:compress-arc 不用 JSON 防火墙:为纯文本步骤提供另一版防火墙措辞("只输出纯文本纲要"),或该步走 creative/无 suffix 信封;至少对 writeArc 前的 text 做围栏/JSON 剥离。
- **验证摘录**:逐行核对确认指控属实,无法驳倒。

复现路径完整可达:
1. 触发条件:config.deepestPromptScope="all"(src/config.ts:22,合法枚举值,SPEC §5 明示可配)+ masterPrompt 非空 + meta.deepestPromptEnabled(src/engine/context.ts:40-41)。这…

### 12. [server-web] /api/books/:book/run 的连写路径缺预算护栏,server 侧可无限连写超支

- **位置**:`src/server/index.ts` 57-69
- **证据**:server 的 write runner 只把 `{ ...deps, onUsage, onDelta }` 转发给 writeMany(第 63-68 行),没有传 `runBudgetUsd` 与 `costProbe`。相比之下 src/cli/index.ts:256-257 明确写了 `runBudgetUsd: loaded.config.singleBudgetUsd, costProbe: () => summarizeUsage(store.dir).totalCostUsd - startCost`。而 writeMany 的预算判定仅当 `deps.runBudgetUsd !== undefined && deps.costProbe`(many.ts:100)才生效。
- **影响**:SPEC §5 与 config.json 里的 singleBudgetUsd 是唯一止损。网页触发 write 3..80 时,预算完全不生效,可能烧穿账户;这与 CLI 行为不一致。
- **建议修法**:在 RUNNERS.write 的 writeMany 分支里补上 `runBudgetUsd: deps.config.singleBudgetUsd`,以及在 SSE runner 开始时 snapshot 一次 `summarizeUsage(store.dir).totalCostUsd`,`costProbe` 返回 `summarizeUsage(store.dir).totalCostUsd - snapshot`(与 CLI 对齐)。
- **验证摘录**:逐行核实成立:server/index.ts:63-68 传给 writeMany 的 wired 由 AppDeps 展开而来,AppDeps=WriteDeps&{chatter,auditor}(deps.ts:11),WriteDeps(write.ts:29-44)不含 runBudgetUsd/costProbe,故 many.ts:100 的 …

### 13. [server-web] Workspace 前端切章不问一句直接丢稿(dirty=true 时也无提示)

- **位置**:`web/src/Workspace.tsx` 48-64, 129
- **证据**:`open` 里没有对 dirty 的检查:`if (sel.kind === 'chapter') { const ch = await api.readChapter(...); setContent(ch.text); ... } setDirty(false);`(48-63)。sideItem 直接 `onClick={() => void open(sel)}`(129)。
- **影响**:用户在编辑器里改了半天章节正文没保存,一手贱点了侧栏另一章,setContent 立即覆盖 CodeMirror,原稿丢失且无法恢复(server 侧也没暂存)。写作场景里这是相当昂贵的丢数据。
- **建议修法**:在 open 开头加一句:`if (dirty && !confirm('当前内容尚未保存,切换将丢失,确认切走?')) return;`(或用受控的 modal)。并把 dirty 挂进 useEffect(beforeunload) 里以拦截关闭浏览器。
- **验证摘录**:证据逐行核实成立:web/src/Workspace.tsx 的 open(48-64 行)对 dirty 零检查,fetch 后直接 setContent 覆盖受控 CodeMirror(181-189 行 value={content})并 setDirty(false);侧栏项 onClick 直连 void open(sel)(129 行)。全 we…

### 14. [server-web] runWorkflow 内部无 try/catch,fetch/流读异常会让 Workspace 的 running 永远卡在 true(按钮永久 disabled)

- **位置**:`web/src/api.ts` 84-122
- **证据**:`runWorkflow` 从 `res.body.getReader()` 开始 for(;;)(第 95-121 行)整段没有 try/catch;对应 Workspace.tsx:108-119 里 `setRunning(true); await runWorkflow(...); setRunning(false);` 也没有 try/finally。
- **影响**:网络中断/服务器崩/reader 抛异常时,setRunning(false) 永远不到,前端"运行"按钮永久灰化(disabled={running}),用户必须刷新页面。用户还看不到任何 onError 提示(reader 循环直接抛出终止)。
- **建议修法**:runWorkflow 内部包一层 try/catch,把异常路由到 `handlers.onError`;或者在 Workspace 里 `try { await runWorkflow(...) } catch (e) { appendLog({ cls: 'err', text: String(e) }); } finally { setRunning(false); }`。
- **验证摘录**:逐行核对确认:api.ts runWorkflow(79-122行)整段无 try/catch——初始 fetch(85行)在服务器不可达时 reject,reader.read()(99行)在连接中断/服务器崩溃时按 Fetch 规范 reject TypeError,两者都直接抛出且不经过 handlers.onError(onError 仅在 92 行…

### 15. [store] 角色名/世界书题直接拼文件路径:路径穿越与 Windows 非法字符可写坏书目录

- **位置**:`src/store/book.ts` 257, 304, 161-164
- **证据**:upsertCharacter: `const file = this.p("角色", `${input.name}.md`)`(L257);upsertWorldbook: `const file = this.p("世界书", `${input.title}.md`)`(L304)。name/title 未做任何文件名消毒,而 writeFile 会 `fs.mkdirSync(path.dirname(fullPath), { recursive: true })`(L162)再写入。
- **影响**:name/title 来自 LLM 抽取(以及将来的 SillyTavern 导入),完全不可控:含 "/" 或 "\"(如「赵三/赵老三」)会静默写进 角色/赵三/ 子目录,listCharacters(L297 只 readdirSync 一层)永远看不到 → 记忆静默丢失;name="../设定" 会直接覆盖书根的 设定.md(写坏书);Windows 下含 ":" 会写成 NTFS 备用数据流、含 "?*<>|" 或结尾空格/点则 EINVAL 崩掉写章管线。SPEC §1 约定文件即记忆,这里是记忆层最薄弱的入口。
- **建议修法**:加一个 sanitizeFileName:剔除/替换 [\\/:*?"<>|] 与首尾空格点、拒绝 ".." 与空串;写入前用 path.resolve 断言结果仍在 this.dir 之内,否则抛中文错误(invalid_entity_name)。
- **验证摘录**:逐行核实,指控成立,无法驳倒。

1. 路径拼接确认:src/store/book.ts L257 `this.p("角色", `${input.name}.md`)`、L304 `this.p("世界书", `${input.title}.md`)`,`p()`(L151-153)只是 `path.join(this.dir, ...parts)`,全文件…

### 16. [store] ## 分节 render↔parse 不闭合:节内容含 "## " 行时下次 upsert 静默截断/丢节

- **位置**:`src/store/book.ts` 103-118, 266-272, 282-289, 431-437
- **证据**:parseSections 用 `/^##\s+(.+?)\s*$/gm` 无条件切分(L105),renderSections 直接 `## ${title}\n\n${content}` 拼回(L117),content 未做任何标题转义。readCharacter 还有 `base: sections.get("基底") ?? parsed.content.trim()`(L287)。
- **影响**:LLM 写的角色状态/状态.md 记录值里出现 Markdown 二级标题(如状态含 "## 伤势\n左臂断裂")是常态。写入后下次 parseSections 会把它切成独立节:readCharacter 的 state 被截断到 "## 伤势" 之前,再次 upsertCharacter 只重渲染 基底/当前状态 两节(L266-271),"伤势" 节内容整体丢失;writeRecords(L432-436)同理会把伪节永久固化进 状态.md。这是纯代码路径的渐进式记忆损坏,测试(store-book.test.ts L118-127)只测了干净输入,未覆盖。
- **建议修法**:写入前对节内容做消毒:把行首 `^##(\s)` 降级为 `###$1` 或加转义前缀;或改用显式分隔符解析。同时 readCharacter 的 base 兜底不应吞下含其他节标题的全文。
- **验证摘录**:逐行核对确认指控成立,机制真实存在:

1. **解析/渲染不闭合确凿**:`src/store/book.ts` L105 `parseSections` 用 `/^##\s+(.+?)\s*$/gm` 无条件按行首 `## ` 切分,L117 `renderSections` 直接 `## ${title}\n\n${content}` 拼回,全文件搜不…

### 17. [store] 竖线行格式字段未清洗换行符:伏笔/时间线/问题 一次写入即永久损坏条目

- **位置**:`src/store/book.ts` 8, 370-391, 420, 509
- **证据**:noPipe 只处理竖线:`s.replace(/\|/g, "/").trim()`(L8)。applyForeshadow/appendTimeline/writeIssues 均把 LLM 字段直接拼进单行格式,如 L389 `\`- [${e.status}] ${e.label} | 埋于第${e.chapterNo}章 | ${e.note} | ...\``,label/note/event 含 "\n" 时一行变两行。
- **影响**:LLM 产出的 note/event 带换行很常见。写入后:后半行不匹配行正则被当坏行跳过(L340-343),前半行解析出残缺条目;而 applyForeshadow 每次都是 listForeshadows 全量重渲染回写(L367→L391),残缺解析结果会覆盖原文件——一条脏数据把 伏笔.md 永久改坏,paid 翻转、dedup 全部失效。时间线/问题.md 同病。
- **建议修法**:noPipe 同时替换换行:`s.replace(/[|]/g, "/").replace(/\r?\n/g, " ").trim()`,并对 label 也在 appendTimeline/writeIssues 的所有字段统一走该清洗(目前 applyForeshadow 的 chapterNo 之外字段有洗,timeline 的 participants 有洗,但都没洗换行)。
- **验证摘录**:逐行核对确认指控成立,无法驳倒。

证据链:
1. D:/desktop/scribe2/src/store/book.ts L8:`const noPipe = (s: string) => s.replace(/\|/g, "/").trim();` 只替换竖线,不处理换行符。
2. 数据来源可达:src/engine/write.ts L48-69 的…

### 18. [tests] PLAN Task 2 的重试策略(withRetry/429退避/auth不重试)零单测覆盖,且 usage 回调断言空洞

- **位置**:`tests/unit/llm-call.test.ts` 20-33
- **证据**:PLAN Task 2 Step 3 明确要求「重试:429/5xx/网络错误指数退避 ×2,abort/auth 不重试(独立小函数 withRetry)」和「cachedTokens 从 providerMetadata 的 prompt_cache_hit_tokens 读,读不到给 0」。实现存在(src/llm/retry.ts 导出 classifyLlmError/nextRetryDelay/withRetry,call.ts:68 调用 withRetry),但全 tests/unit 目录 grep 'retry|429' 仅命中 llm-json.test.ts 的无关变量名,retry.ts 无任何测试。同时 llm-call.test.ts:32 对 usage 只断言 expect(usages).toHaveLength(1)——stub 明明发了 {promptTokens:10,completionTokens:2},却不校验回调值内容,cachedTokens 映射逻辑(providerMetadata 读取)完全未验证,测试名「完成后回调 usage」名不符实。
- **影响**:重试分类(auth 不重试/429 指数退避)是 live 场景的钱袋与稳定性防线,回归时(如误把 401 也重试)单测全绿;usage/cachedTokens 记账错了 status 成本统计与预算检查全部失真,单测同样发现不了。
- **建议修法**:新增 tests/unit/llm-retry.test.ts:用 sleepImpl 注入的假 sleep 断言 429 错误重试 2 次后成功、auth 错误立即抛不调 sleep、nextRetryDelay 指数递增;llm-call 测试的 finish part 加 providerMetadata:{openaiCompatible:{prompt_cache_hit_tokens:4}},断言 onUsage 收到 {promptTokens:10,completionTokens:2,cachedTokens:4},以及无 metadata 时 cachedTokens===0。
- **验证摘录**:事实全部核实成立:(1) src/llm/retry.ts 导出的 withRetry/classifyLlmError/nextRetryDelay(含专为可测性注入的 sleepImpl,retry.ts:66)在整个 tests/ 目录中零引用,grep 'withRetry|429|providerMetadata|readDeepSeekUsage…

### 19. [tests] PLAN Task 14 要求的 CLI 预算检查(budget_exceeded)无任何测试覆盖

- **位置**:`tests/unit/cli-smoke.test.ts` 19-48
- **证据**:PLAN Task 14:「装配生产 deps(…预算检查:估算超 singleBudgetUsd 拒绝)」。实现在 src/cli/index.ts:57-67 checkBudget(历史章均成本×本次章数,超限 fail 'budget_exceeded'),且有 written===0 || totalCostUsd===0 早退分支。cli-smoke.test.ts 只有 3 条用例(--help / book_not_found / bad_chapter_range),grep 全测试目录 'budget_exceeded' 零命中。engine-many-fix.test.ts:134 的成本超限用例测的是 writeMany 内部 costProbe 护栏,不是 CLI 的估算公式。
- **影响**:估算公式或早退条件写反(例如均成本除错分母、比较符号写反)不会被任何测试捕获;这是防止用户一条命令烧穿钱包的唯一闸门。
- **建议修法**:为 checkBudget 抽出可单测的纯函数(传入 summary/written/chapterCount/limit),补用例:超限抛含 budget_exceeded 的中文错、无历史成本放行、恰好等于上限放行;或在 cli-smoke 里造带 usage.jsonl 的假书跑 write 断言退出码非 0 且 stderr 含 budget_exceeded。
- **验证摘录**:事实核验成立:src/cli/index.ts:57-67 的 checkBudget(历史均成本×章数估算)确无任何测试覆盖——全仓 grep budget_exceeded/checkBudget 在 tests/ 目录零命中,cli-smoke.test.ts 仅 3 条用例;engine-many-fix.test.ts:134 测的是 writeM…


## MINOR

### 20. [cli-config] 书名未做路径校验,`../` 可穿越 books/ 目录:resolveBook/new/export 均可读写任意路径

- **位置**:`src/cli/index.ts` 30-36, 170-174, 400
- **证据**:`const dir = path.join(process.cwd(), "books", name);` 对 name 无任何过滤;`new` 命令同样拼接后直接 `BookStore.create(dir)` + `initRepo(dir)`;export 的默认输出 `path.resolve(opts.out ?? `${bookName}...`)` 也把书名拼进文件路径。`scribe new ../../foo` 会在 books 之外任意目录落骨架并 git init,`scribe export ..%2F..` 类书名可把导出文件写到工作区外。
- **影响**:本地 CLI 场景下主要是自伤:误敲或脚本拼接出带 `/`、`..` 的书名会在意料之外的目录建库/初始化 git/覆盖写文件(export 用 writeFileSync 直接覆盖同名文件)。若 server 端复用同一 resolveBook 语义则升级为真实路径穿越面。
- **建议修法**:统一校验书名:仅允许不含路径分隔符的单段名(如 /^[^\\/]+$/ 且排除 `.`、`..`),或 resolve 后断言结果仍在 `books/` 前缀内。
- **验证摘录**:逐行核实成立:src/cli/index.ts:30-36 resolveBook 对书名裸拼 path.join 且仅 existsSync 检查;:170-174 new 命令直接 BookStore.create + initRepo + commitAll,`scribe new ../../foo` 可在 books/ 外建目录并 git init…

### 21. [cli-config] config 加载时不校验 roles[*].provider 是否存在于 providers,unknown_provider 爆点推迟到用户输完话之后

- **位置**:`src/config.ts` 11-25, 101-113
- **证据**:configSchema 只校验 roleModelSchema 的 provider 是非空字符串;loadConfigFrom:90-96 只遍历 `config.providers` 查密钥,从不反向检查 `roles.*.provider ∈ providers`。apiKeyFor 才抛 `unknown_provider`,而它要等 buildDeps→modelFor 时才被调用。onboardLoop(cli/index.ts:148)把 buildDeps 放在**用户输入一整段设定之后**才执行。
- **影响**:roles.writer.provider 写错名字时,`scribe new` 照样建库、打招呼、等输入;用户敲完一大段设定按回车才崩(经 parseAsync().catch 整进程退出),该轮输入白打。错误本应在 loadConfigFrom 就以 config_invalid 报出。
- **建议修法**:loadConfigFrom 里解析成功后遍历 config.roles,对每个非空 role 校验 provider 在 config.providers 中存在,否则立即抛 config_invalid。
- **验证摘录**:逐行核对确认：src/config.ts:11-25 的 configSchema 只校验 roles.*.provider 为非空字符串，loadConfigFrom 90-96 行仅遍历 providers 查密钥，从不校验 roles.*.provider 存在于 providers。unknown_provider 实际抛在 src/llm/prov…

### 22. [cli-config] onboard/chat 循环以空行为退出条件,管道输入中的空行会静默截断剩余轮次,TTY 下误按回车直接结束会话

- **位置**:`src/cli/index.ts` 117-135, 145-147, 213-215
- **证据**:`if (!message || message === "退出" || message === "exit") break;` 而 inputLines 非 TTY 分支整读 stdin 后 `split(/\r?\n/)` 逐行 yield——文件末尾换行产生的 "" 以及正文中任何空白分隔行都会命中 `!message` 直接 break。
- **影响**:脚本化建书(live 剧本/用户管道喂多轮设定)时,输入文件里一个用于分段的空行就让后续所有轮次被静默丢弃,只打印『建书对话结束』,用户以为设定已录入;TTY 下手滑空回车同样无确认即退出。属于两态(TTY/管道)行为差异叠加的边角坑。
- **建议修法**:管道分支跳过空行(continue)而非 break;退出仅认『退出/exit』与流结束,TTY 空回车重新 prompt。
- **验证摘录**:逐行核实了 src/cli/index.ts:117-135 的 inputLines 与两处循环(145-147 onboardLoop、213-215 chat)。指控与代码完全吻合:非 TTY 分支整读 stdin 后 `split(/\r?\n/)` 会把正文中的空白分隔行和文件末尾换行都产出为 ""(第 121 行),而消费侧 `if (!mess…

### 23. [llm] classifyLlmError 用 msg.includes("429") 子串匹配,误把含 "429" 数字的任意错误当 rate_limit 重试

- **位置**:`src/llm/retry.ts` 20-24
- **证据**:`if (status === 429 || msg.includes("429") || msg.includes("rate limit") || ...) return "rate_limit";` —— message 中任何位置出现子串 "429" 都命中,例如 context 溢出类消息 "your messages resulted in 14290 tokens"、含 429 的请求 id/trace id。且该分支在 context_overflow 判断(L36-43)之前,优先级更高。
- **影响**:本应零重试的 context_overflow/unknown 错误被当 rate_limit 重试 3 次(1s/2s/4s),浪费 7 秒 + 3 次计费请求;对确定性失败(参数错误)同样白重试。另注意:非英文错误消息(如 call.ts:16 合成的 "未知流错误(stream_error)"、中转网关返回的中文报错)全部落 unknown 不重试,瞬时网络错误可能因此不被重试——分类完全依赖英文子串。
- **建议修法**:429 判定只信 statusCode;若必须解析 message,用词边界正则(如 /\b429\b/ 配合 "status"/"code" 上下文),并把 context_overflow 判定提到 rate_limit 之前。
- **验证摘录**:逐行核对 src/llm/retry.ts,指控属实,无法驳倒:

1. 代码行为与指控一致:L19-26 `if (status === 429 || msg.includes("429") || ...) return "rate_limit"`,msg 是 `String(err?.message).toLowerCase()`(L17),任意位置含子…

### 24. [llm] 直构 chat model 未开 includeUsage,非 DeepSeek 的流式调用 usage 为 NaN 且 ?? 0 挡不住,成本账本静默失真

- **位置**:`src/llm/provider.ts` 27-37
- **证据**:new OpenAICompatibleChatLanguageModel(modelId, {}, { provider, url, headers, metadataExtractor, defaultObjectGenerationMode }) 的 config 未设 includeUsage,doStream 不会带 `stream_options: { include_usage: true }`;严格 OpenAI 兼容的 provider 流式响应就不含 usage,ai@4 的 result.usage 解析为 NaN。而 call.ts:24-25 `usage.promptTokens ?? 0` 只挡 null/undefined,NaN 直接透传,JSON.stringify(NaN)→null 写进 usage.jsonl,summarizeUsage 再把 null 按 0 计。
- **影响**:换用/新增 DeepSeek 以外的 OpenAI 兼容 provider(五角色本就设计为可配)时,流式路径的 token/成本统计静默归零,账本失真且无任何报错。DeepSeek 恰好总在末块带 usage,所以 live 测试发现不了。降 severity 说明:includeUsage 在 @ai-sdk/openai-compatible@0.2.x 的确切默认值未读 node_modules 验证,但 call.ts 的 NaN 穿透(?? 不挡 NaN)是确定的。
- **建议修法**:config 加 `includeUsage: true`;toUsage 改用 Number.isFinite 兜底:`Number.isFinite(v) ? v : 0`。
- **验证摘录**:逐环节验证成立:(1) provider.ts:27-37 直构 OpenAICompatibleChatLanguageModel 时 config 为 {},未设 includeUsage;(2) 从 GitHub 核对锁定版本 @ai-sdk/openai-compatible@0.2.16(pnpm-lock.yaml:48)源码:L388 仅当 c…

### 25. [memory] 写章增量索引的 changedIds 未复用 store 的归一化(noPipe/trim/normalizeLabel),id 失配导致新增块无向量、paid 伏笔沿用旧向量

- **位置**:`src/engine/write.ts` 294-296(配合 book.ts:8,370-386,420;chunks.ts:43-60;index.ts:82-90)
- **证据**:write.ts:296 timeline 的变更 id 用 extractor 原始输出拼:`timeline:${t.storyTime}|${t.event}`;但落盘时 book.ts:420 appendTimeline 写的是 `noPipe(entry.storyTime)`/`noPipe(entry.event)`(book.ts:8:替换 `|`→`/` 并 trim),chunks.ts:54 的 chunk id 又是从解析后的值再拼——只要 LLM 输出含竖线或首尾空白,changedIds 里的 id 与索引里的 id 就对不上。同理 write.ts:295 `foreshadow:${l}` 用 paid 原始标签,而 book.ts:381-386 applyForeshadow 用 normalizeLabel(NFKC+去全部空白)宽松匹配存量条目:标签有全半角/空格差异时,条目状态照样翻成 paid、chunk 文本从「未回收」变「已回收」(chunks.ts:45),但该 id 不在 target 集合里,index.ts:85-88 走 else 分支把 prev.embedding 原样带给新文本——正是「文本变了却沿用旧向量」;新增 timeline/foreshadow 块 id 失配时则 prev 不存在,落成无向量块,直到下次 rebuildIndex 才补上。record 的节名若带首尾空白也同族(write.ts:293 用原始 key,parseSections 存的是 trim 后的节名)。syncIndex 按文本 diff 算 id,不受此影响。
- **影响**:配置了 embedding 的书,新写章节的时间线/伏笔块可能长期无向量(语义检索捞不到),已回收伏笔携带过期向量;关键词路径仍可用,reindex 可自愈,故降为 minor。
- **建议修法**:changedIds 不要自己拼:落盘后从 store 反查(如 chunksFromBook 后按类型+归一化标签匹配),或最省事——写章场景直接改调 syncIndex(store, embedder),由文本 diff 决定重嵌入哪些块,天然与落盘归一化一致且每章变更量本就只有 5-15 条。
- **验证摘录**:逐行核对了 write.ts:287-298、book.ts:8/368-392/415-423/431-437/103-114、chunks.ts:43-61、index.ts:66-93。指控的三条 id 失配路径全部成立:(1) timeline changedIds 用 extractor 原始 storyTime/event 拼,落盘走 noPip…

### 26. [memory] loadIndex 坏行守卫只验 id/text,缺 keys/type 的合法 JSON 行会让 retrieve 直接 TypeError 崩掉写章管线

- **位置**:`src/memory/index.ts` 28(配合 retrieve.ts:64,105,132)
- **证据**:index.ts:28 守卫仅 `typeof parsed.id === "string" && typeof parsed.text === "string"` 就 push,keys/type/updatedAt 均不验。这样的行进入索引后,retrieve.ts:105 对每个 chunk 调 keywordScore,retrieve.ts:64 `keys.some(...)` 在 keys 为 undefined 时抛 `TypeError: Cannot read properties of undefined`——而 loadIndex 在 writeChapter 第②步(write.ts:161)被调用,整章写作失败,与本文件注释「索引随时可 rebuild,不值得为它崩溃」的自我承诺相悖。type 非法时 retrieve.ts:132 `quotas[entry.chunk.type]` 得 undefined,`used >= undefined` 恒 false,该行还绕过全部配额。不确定性说明:JSON.parse 能挡住截断行,触发需要「合法 JSON 但缺字段」的行(手工编辑 chunks.jsonl 或未来格式漂移),概率不高,故降为 minor。
- **影响**:一行畸形索引让 write/chat/audit 全部以晦涩 TypeError 崩溃,而设计意图是坏行静默跳过、索引可自愈。
- **建议修法**:守卫补齐:`Array.isArray(parsed.keys) && typeof parsed.type === "string"`(不满足即跳过该行),与坏行容错的既有语义一致,一行改动。
- **验证摘录**:逐行核实:index.ts:28 守卫确实只验 id/text;retrieve.ts:64 在 keys 为 undefined 且 entities 非空时抛 TypeError(写章路径 write.ts:161→160 的 entities 来自 plan,正常非空);character_state 行在 retrieve.ts:117 无条件崩;c…

### 27. [memory] keywordScore 对超短 key 无长度下限,单字 key 双向子串命中造成系统性误伤

- **位置**:`src/memory/retrieve.ts` 59-67(对比 chat.ts:26;sillytavern.ts:84-89)
- **证据**:retrieve.ts:64 `keys.some((k) => k && (k.includes(e) || e.includes(k)))` 对 key 只做真值检查;而同仓的实体识别 entitiesFromText(chat.ts:26)刻意加了 `k.length >= 2` 守卫——两处标准不一致,说明短 key 风险是已知的但打分侧漏了。单字 key 的现实来源:ST 导入世界书 keys 原样入库(sillytavern.ts:84,89,SillyTavern 生态中文卡常见「剑」「宗」「道」级别的单字触发词)、角色单字别名。一个 key="道" 的世界书会被任何含「道」的实体(「知道」「街道」「道歉」)命中,单命中在关键词模式下 relevance = 0.6×0.5 = 0.3,稳过 0.15 阈值,还因 worldbook 静态 recency=1 拿高分。
- **影响**:无关块挤占类型配额(worldbook 仅 5 席)并污染写作上下文;书越依赖 ST 导入素材越明显。
- **建议修法**:keywordScore 中对 `e.includes(k)` 方向要求 k.length >= 2(与 entitiesFromText 对齐);`k.includes(e)` 方向同样约束 e 长度。
- **验证摘录**:逐行复核后无法驳倒该发现,机制在代码中完整成立:

1) 打分侧确无长度守卫:retrieve.ts:64 `keys.some((k) => k && (k.includes(e) || e.includes(k)))`,对 k 只做真值检查,`e.includes(k)` 方向单字 key 会被任何含该字的实体命中。同仓 chat.ts:25 `enti…

### 28. [memory] 索引里只要有一个块带向量,全库切到向量权重,大量无向量块的关键词权重被腰斩

- **位置**:`src/memory/retrieve.ts` 87-96,104-107
- **证据**:retrieve.ts:87 `anyEmbedding = input.index.some((c) => Array.isArray(c.embedding))`——单个带向量的块即触发全局向量模式(wKw 从 0.6 降到 0.35,retrieve.ts:94-96);retrieve.ts:104 无向量块 cos 恒 0,其 relevance 上限从 0.6 掉到 0.35,单关键词命中的块从 0.3 掉到 0.175(贴着 0.15 阈值)。触发路径真实存在:书先在无 embedding 配置下写若干章(live 默认),之后配上 embedding 角色——没有任何代码在配置变化时自动 rebuild,后续每章 updateChunks 只嵌入 5-15 个变更块(index.ts:75-79),存量几千块长期无向量,却按向量模式权重被打压,排名被少数新块碾压;上一条 finding 的 id 失配产出的无向量块同样落入此惩罚。此为混合态下的打分失衡,SPEC §2.4 只定义了纯有/纯无两态,属设计空隙而非明写契约违背,故 minor。
- **影响**:启用 embedding 后未手动 reindex 的过渡期(可能长达几十章),检索质量反而比纯关键词模式差:老块系统性低分,靠近阈值的单命中块濒临出局。
- **建议修法**:按块降级而非全局切换:对无 embedding 的 chunk 单独用 0.6/0.4 权重计算其分数;或最低成本方案——检测到 embedder 已配而索引向量覆盖率低时警告/自动触发 rebuildIndex。
- **验证摘录**:指控核实成立。retrieve.ts:87 的 anyEmbedding 用 some() 全局判定,单个带向量块即切全库到向量权重(wKw 0.6→0.35,行94-96);行104 无向量块 cos 恒 0,relevance 上限被压到 0.35,单命中从 0.3 掉 0.175,数值全部核对无误。触发路径可达:cli/index.ts:237 只在索…

### 29. [prompts] plan-chapter 的 {{弧规划}} 槽注入整份大纲,而非"所属弧",与槽位语义及 SPEC §3.2 输入契约不符

- **位置**:`src/engine/write.ts` 140
- **证据**:write.ts:140 传 `弧规划: store.readDoc("大纲").trim() || "(无大纲,自由发挥)"`,即全书大纲原文。prompts/plan-chapter.md 对应标题是「# 所属弧的规划」,SPEC §3.2 ① 规定入参为「大纲对应弧」。
- **影响**:规划器每章都看到全部后续卷/弧走向,可能提前触碰远期剧情、打乱节奏;书写到几百章时该槽 token 随大纲线性膨胀。功能仍可用,故降为 minor。
- **建议修法**:按当前章号定位所属弧(如按弧序号/章数区间截取大纲对应段)再注入;定位不了时才退回整份大纲。
- **验证摘录**:指控属实。write.ts:140 将整份 store.readDoc("大纲")(SPEC:39 定义为全书"卷→弧→章"层级列表)注入 {{弧规划}} 槽,而 prompts/plan-chapter.md:26 槽位标题为「# 所属弧的规划」、SPEC §3.2 ①(docs/SPEC.md:130)规定入参为「大纲对应弧」。代码中无任何按当前章号定位…

### 30. [server-web] 多个 run 参数校验缺口:章号 NaN 直接落到 engine;错误信息不遵守中文说明(错误码)格式

- **位置**:`src/server/index.ts` 57-94, 197-203
- **证据**:RUNNERS.write:`const to = Number(args['to'] ?? from); if (!Number.isInteger(from) || from < 1 || to < from) throw ...`(第 58-62 行)——但 `to = Number('abc')` = NaN,`NaN < from` 为 false,校验放过,进 writeMany 后循环 `for (no=from; no<=NaN; no++)` 直接不执行,返回 completed:[]。RUNNERS.revise:`chapterNo: Number(args['chapterNo'])`(第 84 行)未验证,NaN 一路带下去。此外 GET /chapters/:no 里 `const no = Number(...)`(第 197 行)也不校验 Integer,只在 catch 里返回 `e.message`(第 201 行),错误可能是英文/非中文说明(错误码)格式。
- **影响**:网页请求非法章号得到"成功但什么也没做"的结果(用户困惑),或不合契约的错误提示;不涉及数据损坏,但违反 SPEC §6 错误契约。
- **建议修法**:在每个 RUNNERS 分支入口加一次 `Number.isInteger(...) && >0` 校验并抛"章号不合法(bad_chapter_range)";GET /chapters/:no 同样在 Number 后先校验,再决定进 store.readChapter。
- **验证摘录**:逐条核实(src/server/index.ts + 下游 + 调用方):

【成立的部分】write runner 的 NaN 缺口确实存在。第 58-62 行:`from` 用 `Number.isInteger` 校验,但 `to` 只有 `to < from` 一个比较;`Number("abc")=NaN`,`NaN < 5 === false`,…

### 31. [store] resetToBefore 前缀匹配有二义:ch100 会命中 ch1000,且取「最新一条」在改写场景下语义可疑

- **位置**:`src/store/git.ts` 52-55
- **证据**:`const entry = log(dir).find((e) => e.message.startsWith(prefix));`(L53)。log 最新在前;pad3 用 padStart 不截断(book.ts L5),第 1000 章 commit 是 "ch1000: ..."。
- **影响**:若调用方传 "ch100"(不带冒号),超过 999 章后会先命中更新的 "ch1000: 写作",硬 reset 掉数百章——丢数据。另外 find 取的是最新一条匹配:若第 12 章在写完 15 章后被改写产生第二条 "ch012: 改写" commit,rollback 12 会 reset 到该改写的父提交,13-15 章仍在,违背 SPEC §1「回滚到第 N 章之前」的级联一致语义。降为 minor 是因为调用方代码尚未落地,可能已带冒号且 SPEC 目标仅几百章;但接口本身不设防。
- **建议修法**:在 resetToBefore 内部强制精确边界匹配(如 `e.message.startsWith(prefix + ":")` 或正则 `^ch(\d+):`比对章号),回滚场景应匹配「最早一条该章 commit」或按章号比较取目标。
- **验证摘录**:机制核实属实：git.ts L53 用 startsWith(prefix) 取 log 最新一条匹配；调用方 export.ts L37 与 many.ts L45 均传裸 `ch${pad3(n)}` 不带冒号。碰撞不必等 1000 章：revise.ts L106 产生 `chNNN-revise:` commit（不限最新章）、server/inde…

### 32. [store] issue stableId 只取 note 前 20 字:同章同类型的不同问题会被静默吞掉

- **位置**:`src/store/book.ts` 514-521
- **证据**:`const id = stableId(`${item.type}|${item.chapterNo}|${item.note.slice(0, 20)}`);`(L520),`if (known.has(id)) continue;`(L521),known 含已 resolved 的条目(L517 用全部 issues)。
- **影响**:审查同一章产出两条同类型、前 20 字相同但后文不同的问题(如「时间线冲突:第七日夜灯会——A 先到」vs「……——B 未出场」)时第二条被静默丢弃,审查结果缺失且无任何提示;且与已 resolved 的旧条目撞 id 时,复发问题无法重新登记为 open。djb2 32 位哈希本身碰撞概率在本量级可忽略,主因是 20 字截断。
- **建议修法**:id 用全量 note 参与哈希(截断只为展示);known 命中且旧条目为 resolved 时翻转回 open 而非 continue。
- **验证摘录**:逐行核实 book.ts L514-529：id 确由 type|章号|note 前 20 字哈希（L520），known 含 resolved 条目（L516-517 用 listAllIssues 全量，L490 两种状态都解析），撞 id 直接 continue（L521）。调用方 audit.ts L124 与 many.ts L46/65/78/8…

### 33. [tests] engine-write 用例 6 测试名与断言不符:名称说「弧/002.md 之后新弧生成」,实际断言的是被覆写的 弧/001.md

- **位置**:`tests/unit/engine-write.test.ts` 173-184
- **证据**:用例名「6) 第10章写完自动触发弧压缩,弧/002.md 之后新弧生成」,但断言是 expect(store.readArc(1)).toBeTruthy() 与 readArc(1) 含「镖单残页」。fixture 预置了 弧/001.md 和 弧/002.md(已确认存在),故 toBeTruthy() 恒真、无鉴别力;实现(write.ts:314 arcNo = chapterNo/10 = 1)写的是弧 1,覆写 fixture 预置的「弧1(1-5 章)」。另外 write.ts:300-307 弧压缩失败只 console.warn 不抛,「压缩失败仍 commit 整章」这一容错行为无测试。
- **影响**:测试名误导后续维护者以为验证了「新弧追加」;弧编号语义(覆写 vs 追加,弧2 覆盖 6-8 章与弧1 覆盖 1-10 章重叠)未被测试钉住;compressor 抛错时章节照常落盘 commit 的路径零覆盖。
- **建议修法**:改名并加断言:明确断言 readArc(1) 不再含 fixture 原文「以药铺为掩护」(证明是覆写而非残留);补一条 compressor 抛错的用例,断言 writeChapter 不抛、ch010 commit 存在、console.warn 被调。
- **验证摘录**:逐行核对属实:(1) 用例名「弧/002.md 之后新弧生成」与实际不符——写第10章时 write.ts:314 arcNo=10/10=1,写入并覆写 fixture 预置的 弧/001.md(该文件确认存在,内容「以药铺为掩护」),并非在弧2之后新增;mock compressor 返回文本自称「弧2纲要」却落入弧1。(2) 第182行 toBeTru…

### 34. [tests] deepestPromptScope 的 "all" 枚举值在全测试套中零覆盖

- **位置**:`tests/unit/engine-context.test.ts` 10, 94-115
- **证据**:Config.deepestPromptScope 类型为 "creative" | "all"(src/types.ts,PLAN T1),但 grep 全 tests/unit 目录 '"all"' 零命中——所有 12 个测试文件的 config 都写死 deepestPromptScope: "creative"。engine-context.test.ts 只测了 creative 注入/书级覆盖/关闭开关三条;compressArc(write.ts)调用 deepestPromptFor("structured", …) 的 scope 判定分支从未被 "all" 值触达。
- **影响**:SPEC 契约(深层提示词作用域)的一半枚举值行为无验证:若 "all" 分支写反(structured 调用不注入或 creative 误注双份),测试全绿;用户在 config.json 里切 "all" 才会踩雷。
- **建议修法**:engine-context.test.ts 补两条:scope="all" 时 structured 类调用(extractor/planner 消息)也带【作者全局要求】;scope="creative" 时 structured 调用不带(负向断言)。
- **验证摘录**:逐项核实,指控成立:
1. src/engine/context.ts:44-47 确有 `config.deepestPromptScope === "all"` 分支(structured 调用注入【作者全局要求】+ 末位 JSON_FIREWALL);
2. 全仓 grep `deepestPromptScope`:tests/ 下(unit 12 处…

### 35. [tests] cli-smoke「--help 列出全部命令」只断言 12 个命令中的 3 个

- **位置**:`tests/unit/cli-smoke.test.ts` 20-30
- **证据**:用例名称是「--help 列出全部命令,输出全中文」,断言却只有 for (const cmd of ["write", "status", "reindex"])——这是 Task 14 时代的命令清单。src/cli/index.ts 现有 12 个 .command()(new/onboard/chat/write/fix/audit/revise/status/import/export/rollback/reindex),其余 9 个命令从帮助里消失(如 commander 注册被误删)测试不会红。
- **影响**:「全部命令」的承诺没有兑现:后续任何命令注册回归(尤其 Task 15-21 增加的 9 个)对冒烟测试不可见;全中文断言(not.toContain("Usage:"))也只覆盖顶层 help,子命令 help 未查。
- **建议修法**:把命令清单改为全量 12 个(或从一处常量导出共享),循环断言 --help 输出全部包含;可再抽查一个子命令 help(如 write --help)无英文残留。
- **验证摘录**:亲自核对属实:tests/unit/cli-smoke.test.ts 第 23 行的断言循环只包含 ["write","status","reindex"] 三个命令,而 src/cli/index.ts 注册了 12 个 .command()(166/183/191/223/270/283/304/334/358/388/406/425 行)。用例名称承…


## 补充(主会话人工复核)

### 37. [server-web] run 工作流无取消机制,客户端断连后连写照常烧钱

- **位置**:`src/server/index.ts` run 端点
- **证据**:SSE runner 未接请求的 AbortSignal,也没有任何取消端点;客户端断开后 writeMany 跑完全程。该发现原由评审 agent 提出,两名验证者均因限流失败被自动丢弃,主会话人工复核代码确认属实。
- **影响**:web 端触发 5..80 连写后,关页面/断网都停不下来,叠加「server 无预算护栏」问题,烧钱不可控。
- **建议修法**:run 端点把 `c.req.raw.signal` 织进 deps(streamCall 已支持 abortSignal);另加 POST /api/books/:b/stop 或按连接断开中止。


## 被驳回的指控(防止重复上报)

- **book.md 混入文件白名单,网页端可绕过 writeMeta 篡改元信息(书名 / 深层提示词开关 / masterPrompt / lint)**
  驳回理由:事实核对:指控引用的代码行为属实——FILE_RE 确含 `book`(src/server/index.ts:29-30),PUT /api/books/:book/file 直接 fs.writeFileSync 覆盖(:241),readMeta 从 book.md 中文 frontmatter 读 masterPrompt/深层提示词/lint(src/store/book.ts:168-…
- **SSE 写入队列一旦某次 writeSSE 失败(客户端断连),后续事件与 done/error 静默丢弃,而 runner 仍持续烧钱**
  驳回理由:(两名验证者均遇限流失败,按保守规则丢弃;其中「SSE 断连烧钱」已由人工复核收编为第 37 条)…
- **extractJson 的 balancedCandidates 最坏 O(n²) 扫描,截断 JSON/长文本可造成明显卡顿**
  驳回理由:逐行核对了 D:/desktop/scribe2/src/llm/json.ts:13-37 与调用链(src/llm/call.ts、engine/write|audit|onboard.ts)。代码结构与指控描述一致(每个 `{` 起点各起一次内层扫描),但按发现给出的复现路径推演,量级结论不成立:

1. 主打场景"max_tokens 截断的 JSON"复现失败。截断 JSON 中,绝大多…
- **退避 sleep 不感知 abort:用户取消后仍睡满退避,醒来还会再发一次注定失败的请求**
  驳回理由:代码层面指控属实但场景不可达。核对结论：

1. call.ts:57-61 确实只在进 catch 时检查 `input.abortSignal?.aborted`，`await sleep(delay)` 本身不监听 signal；withRetry（retry.ts:70-84）也确实没有 signal 参数。这部分描述准确。

2. 但复现路径不存在：全仓库 grep `abort|Abo…
- **onboard 的 {{已有设定}} 不含书名,{{缺失项}} 也不覆盖书名,助手可能反复追问已确认的书名**
  驳回理由:指控的事实基础(knownFactsOf 不含 meta.name、readiness 不含书名)属实,但所述影响不成立:prompts/onboard.md 第 7-8 行规定的追问要素清单是"题材与爽点/主角/世界观规则/视角/第一卷走向",书名不在其中,"每轮只追问最关键缺口"追的是这五项,提示词从不驱动模型确认书名;且书名由 `scribe new <书名>` 作为必填 CLI 参数在对话…

## 修复优先级建议(主会话综合)

**P0 —— 动笔前必修(丢数据/写坏书面):**
1. 落盘原子性(#1)+ fixLatest 失败回滚(#2):落盘段包 try,失败 `git checkout -- . && git clean -fd` 回滚到干净态;fixLatest reset 前记录 HEAD,失败恢复。一并解决 draft 收编(major)。
2. 存储层写入消毒三件套(store 3 条 major):实体名做文件名消毒 + 节内容行首 `##` 降级 + 行格式字段清洗换行。一个 `sanitize` 入口 + 单测即可全收。

**P1 —— 影响核心功能质量:**
3. LLM 双层重试(`maxRetries: 0`)。
4. issue 块检索死代码(keys 加中文实体或跳过阈值)。
5. extract-memory 注入现有伏笔清单(伏笔回收契约)。
6. server 连写预算护栏 + 无取消机制(与 CLI 对齐)。
7. guardAudit 第二轮结果丢弃 + 弧覆盖 length*10 空洞。
8. checkBudget 混账 + costOf 单一价目(预算链两条 major)。
9. compress-arc 的 JSON 防火墙冲突(scope=all 时)。

**P2 —— 边角与体验:**
- web:切章 dirty 拦截、runWorkflow try/finally、run 参数 NaN 校验。
- memory:changedIds 归一化(改调 syncIndex 一步到位)、loadIndex 守卫、短 key 下限、混合向量态权重。
- store:resetToBefore 前缀边界、issue id 截断。
- cli:书名路径校验、roles→providers 加载期校验、空行退出语义、classifyLlmError 429 子串。
- tests:重试/预算/scope=all/--help 全量 四个缺口 + 弧压缩用例改名。

---

## 修复记录(2026-07-03,评审后立即执行)

全部 P0/P1 与绝大多数 P2 已修,unit 180 绿 + live 5 绿(真 DeepSeek 全流程复验两轮)。

| commit | 覆盖 |
|---|---|
| `d63f4ee` | store 消毒三件套(#7/#8/#9)+ resetToBefore 边界(#20)+ issue id(#21) |
| `cc344f0` | 落盘原子性(#1)+ fixLatest 回滚(#2)+ 护栏第二轮(#12)+ 弧空洞(#13)+ draft 清理(#14)+ changedIds→syncIndex(#23) |
| `99b15c5` | SDK 双层重试(#10)+ 分类加固(#16)+ usage NaN/includeUsage(#17)+ 重试单测缺口(#32) |
| `a53cfca` | issue 检索死角(#3)+ loadIndex 守卫(#24)+ 短 key(#25)+ 混合权重(#26) |
| `4dd329f` | checkBudget 混账(#4)+ costOf 价目(#5)+ 书名校验(#22)+ roles 校验(#27)+ 空行退出(#28)+ 预算单测缺口(#33) |
| `a89acf4` | 伏笔回收契约(#6)+ compress-arc 文本防火墙(#11)+ 弧规划切片(#19)+ scope=all 测试缺口(#35) |
| `7492365` | server 连写预算(#15)+ 断连取消(#37)+ run 参数校验(#31)+ web 丢稿(#29)/卡死(#30) |
| (随 B/H) | 弧压缩用例改名断言(#34)+ cli-smoke 全量命令(#36) |

未修(有意留待):#18(revise 选段重复出现的锚点体验属 web 交互增强)——现有 occurrenceIndex 机制可用。
